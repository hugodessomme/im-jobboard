// "use server"

// import { db } from "@/lib/db"

// export async function createBoilerplate(values) {
//   const data = await db.boilerplate.create({
//     data: values,
//   })

//   return data
// }

// export async function createManyBoilerplates(values) {
//   const data = await db.boilerplate.createMany({
//     data: values,
//   })

//   return data
// }

// export async function deleteBoilerplate(id: string) {
//   const data = await db.boilerplate.delete({
//     where: {
//       id,
//     },
//   })

//   return data
// }

// export async function deleteManyBoilerplates() {
//   const data = await db.boilerplate.deleteMany({})

//   return data
// }
